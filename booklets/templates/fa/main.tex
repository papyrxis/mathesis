%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode
\documentclass[11pt,oneside,openany]{book}

\input{@CONFIGS_PATH@/colors}
\input{@CONFIGS_PATH@/boxes}
\input{@CONFIGS_PATH@/env}
\input{@CONFIGS_PATH@/math}
\input{@CONFIGS_PATH@/code}

\input{meta}
\usepackage[a5paper,margin=2.2cm,headheight=22pt]{geometry}
\usepackage{microtype}
\emergencystretch=3em
\tolerance=4000
\hbadness=4000
\input{@CONFIGS_PATH@/bibliography}
\addbibresource{@BIB_PATH@}
\usepackage[breaklinks=true]{hyperref}
\input{@BOOKLETS_CONFIGS_PATH@/pagestyles}

\usepackage{pdfpages}

\hypersetup{
  colorlinks=true,
  linkcolor=primary,
  citecolor=primary,
  urlcolor=accent,
  pdftitle={\BookletPDFTitle},
  pdfauthor={\BookletPDFAuthor},
  pdfsubject={\BookletPDFSubject},
  pdfkeywords={\BookletPDFKeywords}
}
\usepackage{xepersian}
\settextfont[
  Scale=1,
  BoldFont=Vazirmatn-Bold.ttf,
  Path=/usr/share/fonts/truetype/vazirmatn/
]{Vazirmatn-Regular.ttf}
\setlatintextfont[
  Scale=0.95,
  Ligatures=TeX,
  BoldFont=FreeSerifBold.ttf,
  ItalicFont=FreeSerifItalic.ttf,
  BoldItalicFont=FreeSerifBoldItalic.ttf,
  Path=/usr/share/fonts/truetype/freefont/
]{FreeSerif.ttf}
\setdigitfont[
  Path=/usr/share/fonts/truetype/vazirmatn/,
  BoldFont=Vazirmatn-Bold.ttf
]{Vazirmatn-Regular.ttf}

% Cross-language convenience macros (\lr, \rl already exist via xepersian
% above; this only adds \ltr / \rtl aliases — see configs/commands.tex).
\input{@CONFIGS_PATH@/commands}

\renewcommand{\BookletChapterWord}{فصل}

% xepersian's book-xepersian.def (loaded by \usepackage{xepersian} above)
% redefines \frontmatter itself — to switch page numbering to harfi — which
% silently discards the \pagestyle{frontmatter} + \fancypagestyle{plain}
% chaining booklets/configs/pagestyles.tex set up before xepersian loaded.
% Re-apply that chaining on top of xepersian's version now.
\let\oldfrontmatter\frontmatter
\renewcommand{\frontmatter}{%
  \oldfrontmatter
  \pagestyle{frontmatter}
  \fancypagestyle{plain}{\MathesisFrontmatterStyle}
}

\providecommand{\frontmatterpagenumber}{}
\providecommand{\mainmatterpagenumber}{}

\DeclareFieldFormat{url}{\lr{\url{#1}}}
\DeclareFieldFormat{urldate}{\lr{(visited on #1)}}
\renewcommand*{\mkbibfootnote}[1]{\LTRfootnote{#1}}

\renewcommand{\headerfont}{\bfseries\small}
\renewcommand{\footerfont}{\scriptsize}
\renewcommand{\pagenumberfont}{\bfseries\small}

\renewcommand{\sffamily}{\fontfamily{\rmdefault}\selectfont}
\AtEveryBibitem{%
  \iffieldequalstr{langid}{farsi}{}{\setLTR}%
}

\begin{document}

\frontmatter
\frontmatterpagenumber
\input{@SHARED_PATH@/cover/cover}
\BookletCover{frontmatter/cover}
\input{@SHARED_PATH@/fa/frontmatter/title}
\input{@SHARED_PATH@/fa/frontmatter/copyright}
\tableofcontents
\input{@SHARED_PATH@/fa/frontmatter/about-mathesis}
\input{@SHARED_PATH@/fa/frontmatter/about-author}
\input{@SHARED_PATH@/fa/frontmatter/acknowledgments}
\input{@SHARED_PATH@/fa/frontmatter/how-to-read}
\input{frontmatter/preface}

\mainmatter
\mainmatterpagenumber
@CHAPTERS@

\backmatter
\InputIfFileExists{backmatter/conclusion}{}{}
\input{backmatter/note}
\input{@SHARED_PATH@/fa/backmatter/references}
\InputIfFileExists{backmatter/glossary}{}{}

\end{document}
